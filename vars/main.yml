---

dockerhub_login_vault: "{{ dockerhub_login }}"

dockerhub_pwd_vault: "{{ dockerhub_pwd }}"

github_private_key: "{{ github_private_key_vault }}"


home: "/home/centos"

fake_backend_source_branch: "master"


repo_fake_backend: "git@github.com:franck-art/projet_jenkins_fake-backend.git"

tag_version: "jenkins_cd"

branch_clone: "master"
